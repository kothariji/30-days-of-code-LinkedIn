#include <bits/stdc++.h>		//day2of30
using namespace std;
int main(){

   ios_base::sync_with_stdio(false);
    cin.tie(NULL);
   /*
    * After writing above two lines at the beginning of our code,
    * we cam make cin and cout as fast as printf and scanf,
    * but then we'll no longer be able to use printf and scanf.
    */
  return 0;
}
